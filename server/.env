PORT=5000
JWT_SECRET=change_this_in_production
# If you later switch to MySQL, provide DSN here and update the server code accordingly.
# For now, SQLite is used at ./data/app.db

# Optional mail settings for reminders
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=0
SMTP_USER=
SMTP_PASS=
NOTIFY_EMAIL_TO=
